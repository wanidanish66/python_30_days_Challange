# we imort the os module to interact with the operating system
import os
# this will list the contents of the current directory
content = os.listdir() 
# print the contents of the current directory
print("the contents of the current directory are:", content)