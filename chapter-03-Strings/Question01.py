# This code takes the user's name as input and then prints a greeting message with the user's name.

name = input("Enter your name: ")  # takes input from the user and stores it in the variable 'name'
print("good afternoon ", name)  # prints a greeting message along with the user's name